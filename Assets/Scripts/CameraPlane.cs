using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraPlane : MonoBehaviour
{

	// Use this for initialization
	void Start ()
    {

    }

    // Update is called once per frame
    void Update ()
    {
		
	}

    public void TeleportHandler(Transform _targetTransform)
    {
        //move player to position and rotation of remote camera (excluding Y value so the player doesn't end up in the floor
    }
}
